description "flowLong mybatis-plus solon plugin"

dependencies {
    api project(":flowlong-core")

    compileOnly("org.noear:mybatis-plus-extension-solon-plugin:${solonVersion}")
    compileOnly("org.noear:solon-api:${solonVersion}")

    testCompileOnly("org.noear:solon-test:${solonVersion}")
    testCompileOnly("mysql:mysql-connector-java")
}
